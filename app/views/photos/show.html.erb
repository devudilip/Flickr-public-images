
<%- model_class = Photo -%>
<div class="page-header">
  <h1><%= @results['title'] %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th> Photo </th>
      <th> Photo Title</th>

    </tr>
  </thead>
  <tbody>
    <% @results['items'].each do |photo| %>
    <tr>
    <td> <img src="<%= get_clean_url(photo['description'])%>.jpg " > </td>

    <td><%= link_to truncate(photo['title']), friends_photos_photo_path(photo['author_id']) %></td>
      
    </tr>
    <% end %>
  </tbody>
</table>


